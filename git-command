✅ Initial Git Setup (if not done yet)
git (confirm whether you installed correctly or not in cmd)
git config --global user.name "seshathri044"
git config --global user.email "your-email@example.com"
🔹 1. Clone a GitHub Repository
git clone https://github.com/your-username/repo-name.git
🔹 2. Change into the project directory
cd repo-name
🔹 3. Create or edit files
Create a file:
notepad hello_world.java
🔹 4. Check file status(optional)
git status
🔹 5. Add file(s) to staging area
git add hello_world.java
Or to add all files:
git add .(enter correctly)
🔹 6. Commit the changes
git commit -m "Add hello_world.java program"
🔹 7. Pull latest changes from remote (important if working with remote repo)
git pull origin main --rebase
🔹 8. Push your code to GitHub
git push origin main (check in your github whether the file is present or not)
